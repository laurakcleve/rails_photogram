$('#notifications').append("<%= j (render @notifications.order('created_at DESC')) %>");

$('#paginator').html("<%= j (link_to_next_page(@notifications, 'Show More', remote: true, id: 'show_more'))%>")