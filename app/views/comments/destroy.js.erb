var comments = $('#comments_<%= @post.id %>');

comments.html("<%= j render @post.comments, post: @post, comment: @comment %>");

if (!comments.children().size()) {
  comments.removeClass('post__comments');
}